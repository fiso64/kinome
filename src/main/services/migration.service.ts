/**
 * Database migrations now live in `src/main/database/migrations/` and are run by
 * `src/main/database/migrations.ts` during database initialization, before any
 * repository query touches the schema.
 *
 * This file remains as a short signpost because older notes and search results
 * may still point at `migration.service.ts`.
 */
